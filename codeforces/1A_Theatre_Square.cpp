#include <math.h>
#include <iostream>
using namespace std;

int main() {
    int n, m, a;
    cin >> n >> m >> a;
    long long x = ceil((float)n/a);
    long long y = ceil((float)m/a);
    return 0;
}
